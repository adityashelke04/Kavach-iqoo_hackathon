# OpenRouter key for the cloud engine (SPEC.md §8.2).
# Optional: the app works without it via the on-device and rules engines.
# Copy this file to .env.local and fill in a key. Never commit .env.local.
VITE_OPENROUTER_API_KEY=
